import { agent as request } from "supertest"; 
import { expect } from 'chai';
import { describe, it } from 'mocha';


//unwritten for now, but once server is abstracted into it's own class we could new it here, run tests then strip it down.
